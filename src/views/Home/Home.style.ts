import { createUseStyles } from "vue-jss";
const styles = createUseStyles({
  blueTxt: {
    fontSize: 40,
    color: "skyblue",
  },
  HomeWrapper: {
    height: "100%",
  },
});
export default styles;
